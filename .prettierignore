# Payload and Next generate these. Formatting them makes every `pnpm dev` and
# `pnpm generate:types` run show up as a spurious modification.
src/payload-types.ts
src/app/(payload)/admin/importMap.js
src/migrations/
pnpm-lock.yaml
.next/
